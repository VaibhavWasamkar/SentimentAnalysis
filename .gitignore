# Node modules - always exclude, can be regenerated with npm install
/node_modules/

# Build output (if using Vite, the default output folder is dist)
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
.DS_Store
Thumbs.db

# Environment variables (if any)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE / Editor folders
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Optional: if you have local test coverage reports
coverage/

# Optional: exclude local images if you don’t want them tracked
/public/images/
